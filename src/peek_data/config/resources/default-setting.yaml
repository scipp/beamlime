# THIS FILE IS AUTO-GENERATED.
# Please don't update it manually.
# Use `tox -e config-build` to generate a new one.

general:
  last-updated:
  log-dir: ~/.peek-data/log/
  name:
  root-dir: ~/.peek-data/
  user:

dashboard:
  css:
  display-targets:
  - index:
    interactive:
    method:
    type:
  update-frequency: 5

data-stream:
  custom-handler-dir: ~/.peek-data/custom-handlers/
  interface-mapping: "-from: \n to:"
  interfaces:
  - kafka:
      chunk-size: 8
      data-handler:
      input-channel:
        config:
          topic: test
        type: kafka
      max-wait: 1
      output-channel:
        config:
        type:
      type:
  - data-reduction:
      chunk-size: 8
      data-handler:
      input-channel:
        config:
        type:
      max-wait: 1
      output-channel:
        config:
        type:
      type:
    type: byte
  - dashboard:
      chunk-size: 8
      data-handler:
      input-channel:
        config:
        type:
      max-wait: 1
      output-channel:
        config:
        type:
      type:

data-reduction:
  input-interface:
  targets:
  workflow-target-mapping:
  - targets:
    workflow:
  - targets:
    workflow:
  workflows:

